# Agenda pública

`/agenda` se mantiene como alias compatible de `/usersCalendar`.

La fuente pública es el calendario de Google `UnderTango Club` (`undertangoclub@gmail.com`) consumido del lado servidor mediante su feed ICS público. La web no publica descripciones internas: sólo título, fecha/hora, ubicación y categoría derivada.
